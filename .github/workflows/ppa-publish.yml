name: PPA Publish

on:
  workflow_dispatch:
    inputs:
      version:
        description: Version to release (e.g. v0.1.0)
        required: true
        type: string
      revision:
        description: Debian revision (default 1)
        required: false
        default: '1'
        type: string
      upload:
        description: Upload to PPA
        required: false
        default: false
        type: boolean

jobs:
  prepare:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
      ref: ${{ steps.ref.outputs.ref }}
    steps:
      - name: Determine git ref
        id: ref
        run: |
          VERSION="${{ github.event.inputs.version }}"
          if [[ "$VERSION" == v* ]]; then
            echo "ref=$VERSION" >> "$GITHUB_OUTPUT"
          else
            echo "ref=v$VERSION" >> "$GITHUB_OUTPUT"
          fi

      - name: Set version
        id: version
        run: |
          VERSION="${{ github.event.inputs.version }}"
          echo "version=${VERSION#v}" >> "$GITHUB_OUTPUT"

      - uses: actions/checkout@v5
        with:
          ref: ${{ steps.ref.outputs.ref }}
          path: source

      - name: Get current debian files
        uses: actions/checkout@v5
        with:
          sparse-checkout: debian
          sparse-checkout-cone-mode: false
          path: debian-files

      - name: Copy current debian packaging
        run: cp -r debian-files/debian source/

      - name: Download release binaries
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          mkdir -p source/prebuilt
          gh release download "v${VERSION}" -R "${{ github.repository }}" -p "nibble_linux_amd64.tar.gz"
          gh release download "v${VERSION}" -R "${{ github.repository }}" -p "nibble_linux_arm64.tar.gz"
          tar -xzf nibble_linux_amd64.tar.gz nibble
          mv nibble source/prebuilt/nibble_linux_amd64
          tar -xzf nibble_linux_arm64.tar.gz nibble
          mv nibble source/prebuilt/nibble_linux_arm64
          chmod +x source/prebuilt/nibble_linux_*
          rm -f nibble_linux_*.tar.gz
        env:
          GH_TOKEN: ${{ github.token }}

      - name: Create orig tarball with prebuilt binaries
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          tar --exclude=source/.git \
              --exclude=source/.github \
              --exclude=source/debian \
              --transform="s,^source,nibble-${VERSION}," \
              -czf nibble_${VERSION}.orig.tar.gz source

      - name: Upload orig tarball
        uses: actions/upload-artifact@v4
        with:
          name: orig-tarball
          path: nibble_${{ steps.version.outputs.version }}.orig.tar.gz
          retention-days: 1

  ppa:
    needs: prepare
    runs-on: ubuntu-latest
    strategy:
      matrix:
        series: [noble, jammy, questing]
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ needs.prepare.outputs.ref }}
          path: source

      - name: Get current debian files
        uses: actions/checkout@v5
        with:
          sparse-checkout: debian
          sparse-checkout-cone-mode: false
          path: debian-files

      - name: Copy current debian packaging
        run: cp -r debian-files/debian source/

      - name: Download release binaries
        run: |
          VERSION="${{ needs.prepare.outputs.version }}"
          mkdir -p source/prebuilt
          gh release download "v${VERSION}" -R "${{ github.repository }}" -p "nibble_linux_amd64.tar.gz"
          gh release download "v${VERSION}" -R "${{ github.repository }}" -p "nibble_linux_arm64.tar.gz"
          tar -xzf nibble_linux_amd64.tar.gz nibble
          mv nibble source/prebuilt/nibble_linux_amd64
          tar -xzf nibble_linux_arm64.tar.gz nibble
          mv nibble source/prebuilt/nibble_linux_arm64
          chmod +x source/prebuilt/nibble_linux_*
          rm -f nibble_linux_*.tar.gz
        env:
          GH_TOKEN: ${{ github.token }}

      - name: Download orig tarball
        uses: actions/download-artifact@v4
        with:
          name: orig-tarball

      - name: Install packaging tools
        run: sudo apt-get update && sudo apt-get install -y devscripts debhelper build-essential dput gnupg

      - name: Import GPG key
        run: |
          echo "${{ secrets.PPA_GPG_PRIVATE_KEY }}" | gpg --batch --import
          GPG_KEY_ID=$(gpg --list-secret-keys --with-colons | awk -F: '/^sec/{print $5; exit}')
          echo "GPG_KEY_ID=$GPG_KEY_ID" >> "$GITHUB_ENV"

      - name: Update debian/changelog
        run: |
          cd source
          VERSION="${{ needs.prepare.outputs.version }}"
          REVISION="${{ github.event.inputs.revision }}"
          DEBEMAIL="mail@saberd.com"
          DEBFULLNAME="saberd"
          export DEBEMAIL DEBFULLNAME
          dch --newversion "${VERSION}-${REVISION}~${{ matrix.series }}" \
              --distribution "${{ matrix.series }}" \
              --force-distribution \
              "Release ${VERSION}"

      - name: Build source package
        run: |
          cd source
          REVISION="${{ github.event.inputs.revision }}"
          if [[ "$REVISION" == "1" ]]; then
            debuild -S -d -sa -k"$GPG_KEY_ID"
          else
            debuild -S -d -sd -k"$GPG_KEY_ID"
          fi

      - name: Upload to PPA
        if: ${{ github.event.inputs.upload == 'true' }}
        run: |
          VERSION="${{ needs.prepare.outputs.version }}"
          REVISION="${{ github.event.inputs.revision }}"
          dput ppa:backendsystems/ppa nibble_${VERSION}-${REVISION}~${{ matrix.series }}_source.changes

      - name: Copy artifacts to current directory
        if: ${{ github.event.inputs.upload != 'true' }}
        run: cp *.{dsc,tar.gz,changes,buildinfo} . 2>/dev/null || true

      - name: Upload artifacts
        if: ${{ github.event.inputs.upload != 'true' }}
        uses: actions/upload-artifact@v4
        with:
          name: ppa-source-${{ matrix.series }}
          path: |
            *.dsc
            *.tar.gz
            *.changes
            *.buildinfo
          retention-days: 7
